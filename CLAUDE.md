@AGENTS.md

# 事業の方向性(最重要メモリー)

このプロジェクトは **煙道 ENDO** ——「**日本のシーシャの"王道"を、みんなでつくる**」共創プラットフォーム。
単なるレシピサイトではなく、**探す→作る→吸う→残す→比べる**のループで、フレーバー・作り方・実際に吸った評価・店舗をつなぎ、日本人の舌に合うシーシャの基準(=王道)を育てる。
**扱う単位は「1つのフレーバーを、どう作るか」**(2026-08-18 改修。後述「アプリの構造」)。

**最上位の指針は `docs/基本方針.md`。実装・意思決定の前に必ず参照すること。**
- `docs/基本方針.md` … コンセプトの背骨(王道の定義・段階開示・店舗ループ・マイ煙道・3つのデータ 等)
- `docs/機能ロードマップ.md` … 全機能を ①ローンチ必須 / ②ローンチ後 / ③将来構想 に整理
- `docs/仕様_評価と称号.md` … 確定仕様(味覚5軸/王道はV1総合1つ/達人 段位・称号)
- **`docs/ローンチ戦略.md` … ローンチ運用の指針**(3フェーズ・初期150method・創設作り手・KPI・公開前チェックリスト)※後述の「広さ vs 深さ」で方向性が競合する箇所あり
- **`docs/プロダクト戦略.md` … 現在の仮説と検討事項**(テーマ固定型・経験量・再現性検証 O/R/TP・グローバル・未決定事項・設計課題14項目)※仮説であり実装ルールではない
- `docs/事業計画.md` … 詳細メモ、`docs/ブランド.md` … 名称・表記の出典
- **`docs/アプリ構造_フレーバー中心.md` … 現在のアプリ構造**(概念とデータの対応・画面・取り除いたもの・旧データの扱い)
- `docs/設計原則_表示モード.md` … 表示モードを持たない理由の詳細(過去の失敗の経緯)

# 上位概念：「道」(2026-08 追記)

ENDOは「美味しいシーシャの正解を教えるアプリ」ではなく、**「シーシャを極めるための道具」**である。

「道」= 正解を最短で覚えることではなく、**型を学び、実践し、違いを知り、繰り返し、自分の型を磨いていく過程**。

現在のコアループ(フレーバーを選ぶ → 他人のMETHODを試す → 一台作る → 記録 → 前の一台と比べる → 次の一台へ)**そのものが「道」の実装**であり、変更しない。比較は表示が目的ではなく、**比較が次の探究を生むこと**が目的。

言い換え(設計判断の前提として扱う):
- ユーザーは「レシピの消費者」ではなく「自分の型を作っていく人」
- ENDOは「正解を固定するアプリ」ではなく「現在分かっていることを土台に擦り続けられるアプリ」

**この思想は現在の設計を広げる口実ではない。** 思想を理由に機能を先回りで作らないこと(後述「機能・UI提案時の判断基準」「今は作らないもの」)。

# 中心思想：同じものを、みんなで何度も擦る(2026-08 更新)

煙道の価値は「誰か一人が考えた美味しいレシピを載せること」ではない。
**同じフレーバーを、多くの人が違うやり方で何度も作り、吸い、評価し、再現し、その結果を蓄積すること**にある。
そこから「本当に再現される作り方 / 実際に支持される作り方 / 経験に裏付けられた作り方」を見つけ、
最終的に「日本のシーシャの"王道"を、みんなでつくる」へつなげる。

したがって **method の投稿数そのものより、実証データの方が重要**：
実際に作られた回数 / 作った人数 / リピート回数 / 再現された回数 / 評価 / Original と Reproduction の比較 / 作り手の経験量。

**煙道は「シーシャ版クックパッド」にしない。** 無数のレシピを投稿して終わりにせず、同じフレーバーを擦り続ける。

言い換え(設計判断の前提として扱う)：
- 一人の正解を集めるのではなく、みんなの試行から最適解を見つける
- プロの価値は肩書きではなく、積み重ねた経験・試行回数にある
- 王道とは、人気のあるレシピではなく、実際に作られ・再現され・繰り返し検証された作り方を煙道が認定したもの

# アプリの構造(2026-08-18 全面改修)
**コア機能＝「1つのフレーバーを、いかに美味しく作るか」。フレーバーの組み合わせという概念は取り除いた。**
- 作り方に紐づくフレーバーは常に**1つ**。配合(ratio)は入力も表示もしない
- その結果 `mixes.combo_key` は**そのままフレーバーのキー**になる。`combo_orthodoxy` は「そのフレーバーの王道」
- 中心画面は `/flavor/[id]`(そのフレーバーをどう作るか)。作り方は `/method/[id]`(旧 `/mix/[id]` は互換リダイレクト)
- 主行動は投稿ではなく**試して記録すること**。下部ナビ中央は「記録」(`/record`)
- 旧モデルの複数フレーバー記録(編集部見本7件)は削除せず `hidden=true` で下げた。`combo_key` に ` + ` を含むものがそれ
- `mixes` というテーブル名・`combo_key` という列名は旧モデルの名残。既存データとRPCが動いているため列名だけ残している

## 将来構想：フレーバーを極めた先にミックスがある(2026-08-18 記録)
**いまミックスを取り除いたのは「ミックスをやらない」という意味ではない。順番の話。**
1つのフレーバーの作り方を極める → **そのフレーバーの特徴（温度帯・熱耐性・味の出方）が分かる** →
その知識の上で初めて上手なミックスが作れる。この順番を作りたい。

したがって**最終的にはミックスも扱う**。ただし既存のミックスサイトと決定的に違うのは、
煙道は**使用フレーバーそれぞれの特徴を把握できている**ので、
**「なぜ相性が良いのか」を具体的に言語化できる**こと。例：
- 温度帯 … AL FAKHER は高い温度帯で美味しい味が出る → 同じく高温で味が出るものと合わせる／中東系が合う
- 盛り方 … 熱に強いフレーバーを炭に近い上へ、熱に弱いアメリカ系を下へ置いてミルフィーユ状に盛る

**この言語化ができる根拠が「1フレーバーを極めたデータ」**なので、順番を飛ばさない。
ミックスの再導入は、フレーバー単体のデータが十分貯まってから。**いま実装しない。**

# 見た目の決めごと(2026-08-19 全面刷新。勝手に戻さない)

**地は暗い方ひとつ。明暗の切り替えは持たない。** 以前の「和紙のクリーム色」は廃止した。
地の色は目分量で決めていない。**ヒーローの実写コマを明度で層に分けて平均を取り、その値を採用している**
(影は冷たい藍緑、中間は温かい茶、最も明るいのが煙)。写真とページを同じ世界に見せるため。

- **アクセントは煙道の緑のまま。橙(`--color-seal`)は「実際に火がある所」だけに出す。** 飾りに使わない
- 「暗い＝黒＋琥珀＋高コントラスト明朝」は、どこにでもある"シネマティック"の型なので避ける。
  地は純黒にしない(`--color-smoke-950: #0b0e0e`)
- **署名要素は「熱の目盛り」(`.gauge` / `.gauge-rule`)。** 何通りで作られたかを数字より先に厚みで見せる。
  セクションの罫にも同じ文法を使う。**このサイトの記号はこれ1つ**。2つ目を足さない
- ロゴは「字の左に一本の煙」(`.mark` / `.mark-flue`)。赤い角丸に白抜きの「煙」は廃止
- **絵文字を使わない(ナビも本文も)。** 線画を使う。ナビは `components/nav-icons.tsx`、
  本文は `components/line-icons.tsx`(名前で引く)。丸に入れるなら漢字一文字でもよい
- **`IconOrb` は色を持たない。** 以前は苔緑・朱土・藍・利休茶の4色で塗り分けていたが、
  意味の無い所に4色が並ぶと「アクセントは緑ひとつ、橙は火の所だけ」が嘘になる。
  地の階調と髪の毛一本の輪郭だけにし、区別は色ではなく中身(漢字か線画)で付ける
- **塗りの上に白文字を置かない。** 緑の塗り(`--color-ember`)に白は 3.27 で不合格。`--on-fill` を使う
- 和柄ユーティリティ(青海波・麻の葉・落款スタンプ・界線)は他ページに残っているが**新規には使わない**
- 隣り合うセクションで同じ骨格を使わない
- **見せるために JS を必要とする作りにしない(最重要)。** 既定は「見えている」。
  隠すのは JS が印を付けた物だけ。逆にすると、JS が動かない・観測が回らない条件で
  本文が丸ごと消える。2026-08-20 に2件やった(`.still` を丸ごと隠す／`[data-rise] > *` を
  無条件に opacity:0)。検証は `.rise-safety.mjs`(JS を切って全ページを見る)

## 画像・動画のプロンプトは「コンセプトの絵」にする(2026-08-19 追加。最重要)
**きれいなシーシャの写真を作るのではない。この CLAUDE.md に書いてある煙道の中身を絵にする。**
プロンプトを書く前に、必ず「中心思想」と「アプリの構造」を読み直すこと。

絵に必ず入れる(＝これが無い絵は却下):
1. **同じフレーバーが、違うやり方で、複数ある**こと。1台だけの絵は「作る」しか言っておらず、
   煙道の中身(＝同じものをみんなで何度も擦る)を1ミリも言っていない
2. **違いは手順で説明できる**こと。器具の差(アルミ/HMD/炭の数と位置/詰め方)が絵で見分けられる
3. **最後は1つに落ち着く**こと。並んだ中の1台に寄って終わる＝「まずこれ」＝王道の視覚化

絵に入れてはいけない:
- レシピ本・料理写真の構図(シーシャ版クックパッドにしないという判断に反する)
- 賑やかな店内・映える演出。煙道は静かに検証する場所
- 実在ブランドのロゴ、文字、レタリング

**用語**: 「吸う」であって「飲む」ではない。器具の順番は ボウル → アルミ → HMD → その上に炭。

## トップのヒーロー(スクロールで駒送り)
`components/scrub-hero.tsx`。縦に長い器の中で1画面が貼り付き、スクロールの進み(0→1)が動画の再生位置になる。
**下へ送る＝トングが下りて炭が置かれる。動きとスクロールの向きを必ず一致させる。**

外すと目に見えて品質が落ちる決まり(全部守ること):
- 動画は Blob で丸ごと取る。部分取得非対応の配信元だと seek が全部0に落ちる(ローカルでは気づけない)
- 形式を mp4 と webm の2つ持ち、`canPlayType` で選ぶ。H.264 を積まないブラウザで丸ごと死ぬ
- スクロール値を `currentTime` に直接書かない。追従させ、追いついたら休む
- seek は1本ずつ。前の seek 中に書くと Chrome で目に見えてガタつく
- DOM への書き込みは値が変わったときだけ
- **動画が来なくてもページは完成している。** ポスターの上で全部読める
- **門**(動きが苦手な設定/寝かせたスマホ)では動画を読み込まない。
  門は CSS と JS で**字句まで一致**させる(片方だけ変えると片側だけ資産を読む)
- **`.still` には2つの使い道がある。取り違えると画面が丸ごと消える。**
  `still still-gate` … スクラブと対の控え。門に当たった時だけ出る／`still` だけ … 単体で常に出す。
  門で隠すのは `.still-gate` のみ。`.still` を隠すと単体ヒーローまで消える
  (2026-08-20 に実際に消した。ログイン後のトップが本番で真っ黒になった)。
  検証は `.still-gate.mjs`。PC/スマホ縦/動きが苦手/寝かせたスマホ の4条件で測る

## 直す前に必ず測る
`.scrub-audit.mjs`(帯ごとの最悪ピクセル・フリック試験) と `.audit.mjs`(全ページのコントラスト・タップ領域)。
**写真や動画の上の文字は、宣言した背景色と比べても意味がない。** 文字を消したページを撮り、
文字の箱の中の実ピクセルの最悪の1点と比べる。目分量でスクリムを濃くしない。

# 設計原則
- どの画面も「結論(王道/まずこの作り方)→ なぜ → 詳細データ」の段階開示
- 評価は"実際に吸った(作った・実地評価)"を最重視。用語はシーシャを「**吸う**」(「飲む」は使わない)
- **ユーザーを分ける永続的な「モード」を作らない。** 情報量の出し分けは画面ごとの段階開示(`<details>`)で行う。選択させるなら毎回選び直せる行動単位のみ。迷ったら「この選択はユーザーに貼られ続けるか?」と問い、貼られ続けるなら作らない(経緯は docs/設計原則_表示モード.md)
- **「作った」の自己申告だけで再現性を断定しない。** 重要な行動データとして扱うが、それ単体で「この method は再現性がある」とは言わない。「再現成功」も人によって基準が違うため、単純な Yes/No を再現性の証明に使わない

# 現在のフェーズ(2026-08〜)
**「もっと機能を作る段階」ではなく、「人とデータを入れて本当に回るかを検証する段階」。**
最大リスクはコードではなく、初期流通量・作り手・experience・taste・王道が足りないこと。大きな新機能追加は原則止め、実データを増やすことを優先。

## 用語(実装と会話で統一)
`flavor`=1つのフレーバー(`flavors`。煙道が扱う対象そのもの)/`method`=そのフレーバー1つをどう作るか(`mixes`の1行。UI表記は「作り方」)/`experience`=実際に吸った・作った体験(`mix_experiences`)/`比較`=前に作った別のmethodとの直接比較(`mix_experiences.comparison`)/**王道=同一フレーバー内で公式認定されたmethod(`combo_orthodoxy` が唯一のsource of truth)。現時点で最も検証が進んでいる作り方であって、人気投票でも唯一の正解でもない。王道はゴールではなく現在地**/`推薦`=認証プロ・運営によるmethod推薦(`method_recommendations`)/`taste`=実際に吸った人による味覚5軸/`型`=作り方を記述する5軸(ボウル / 詰め方 / HMD / 立ち上げの炭 / 初期加熱)。単なるDB項目ではなく「シーシャを擦るための観測可能な型」/`同じくらい`=比較で差が出なかったこと。失敗ではなく「この変数では大きく変わらなかった」という、次の実験を決めるためのデータ

**「ミックス」「配合」「組み合わせ(combo)」はユーザーに見せる言葉としては存在しない。**

## North Star Metric
**月間「吸った」登録数**= `experience_type IN ('smoked','made')`。PVや登録者数だけで成功判断しない。

## 行動原則(迷ったらここに従う)
1. 新機能追加より、実ユーザー・実データを増やす
2. 迷ったら「吸った」体験を最優先
3. 王道を人気投票化しない(いいね数や自動スコアで王道を決めない)
4. 架空の experience / taste を絶対に作らない
5. 初心者に情報を見せすぎない/上級者には段階開示で深く
6. 既存コアループを壊さない
7. 機能を増やす前に、削る・並べ替える・言い換える
8. production 確認までを「実装完了」と定義する
9. 実データが無いと判断できない問題は、机上で作り込みすぎない

## 機能・UI提案時の判断基準(8項目)
新機能・UI変更を提案する前に確認する:
1. 実際に一台を作ることに繋がるか
2. 作った経験を残すことに意味があるか
3. 他の一台との違いを知ることに繋がるか
4. 比較結果が次の一台に繋がるか
5. ユーザー自身の理解・型の形成に繋がるか
6. 「正解を受け取る」だけの体験になっていないか
7. 人気投票・ランキング化で「道」の思想を壊していないか
8. 繰り返し使うことで価値が増える構造になっているか

コピー・UI文言もこの思想に従う。「これが正解」と断定する表現は使わない(王道は常に「現時点の検証結果」として提示する)。

**迷ったら**「この提案は道の思想の**実装**か、思想を**口実にした先回り**か」を自問する。後者なら出さない。

## 今は作らないもの(実データが増えるまで保留)
guest mode/anonymous auth/AI作り方提案/ゲーミフィケーション/段位・称号/新ランキング/高度検索/味覚軸追加/店舗SaaS本格化/複雑な王道自動判定/高度な不正検知/大規模パーソナライズ

「道」の思想から来る保留(2026-08 決定。思想を理由に先回りしない。まず実際に使い込む):
- 予想機能・自動推薦・通知(先回りしない)
- 人気投票・ランキング化
- 王道の急いだ確定
- 目的別最適化(濃厚/軽め等の条件分岐)※将来の検討対象だが、今は5軸・比較・次の一台を擦る段階

**例外: トップの「火を入れる」(2026-08-19 判断)**
ヒーローの長押しで火が入る演出は、上の「ゲーミフィケーション」には**当たらない**ものとして残す。
理由: ポイント・バッジ・ランキング・連続記録など**行動を報酬で誘導する仕組みを一切持たない**。
状態を保存せず、スコアも与えず、次の行動を要求しない。**サイト全体で1つだけ置く"設計されたインタラクション"**であり、
「煙道」という名前を体で分からせるためだけに存在する。
**この例外は1つまで。2つ目を足したくなったら、それはゲーミフィケーションなので作らない。**
**有料化・ノウハウ販売も直近では採用しない**(2026-08 判断)。まずデータネットワーク(ユーザー数・作成数・再現数・リピート数・評価・method数・王道候補)を作る。課金は「煙道にしかない価値・データが十分貯まった後」に再検討。

## 未決定の警戒線(仮説を実装ルールにしない)
以下は **検討中であって決定事項ではない**。仕様として実装しない・既存仕様を上書きしない。詳細と判断材料は `docs/プロダクト戦略.md`。
投稿者のプロ限定化 / 一般ユーザー投稿の禁止 / 有料レシピ / 第一弾テーマの正式決定 / 第三者比較の全method必須化 / 王道認定の数値基準 / 世界展開の具体策 / 「一番美味しい」を絶対的に決める仕組み / 店舗投稿を無条件に高く評価する仕組み

## 方向性の決定(2026-08-18)
**第三案 C を採用。深さ優先は"ブートストラップ"であり最終形ではない。**
- 深さ … 第一テーマを1つ集中検証(第一候補: AL FAKHER ダブルアップル 100%。正式決定は参加者ヒアリング後)
- 広さ … 既存86フレーバー図鑑を維持(新規作成コスト0・捏造0)。**150 method の人為投入は行わない**
- UGC … 自由投稿は閉じない。テーマは「制限」ではなく「集中先」
- **Double Apple だけのサイトにしない。** 表側は通常の煙道、その中に特別エリアとして検証テーマを置く
- 広さは"入力"ではなく"出力"。人為投入で作らず、ルール確立後にユーザーが持ってくる

詳細は `docs/検討_テーマ固定型_第一テーマ設計.md`(分析)と `docs/第一テーマ_ユーザージャーニー設計.md`(設計)。

## 第一実験で証明する問い(唯一つ)
**「煙道では、人が"他人の method"を試して比較するのか?」**
「ダブルアップルの最高の作り方」ではない。YES なら他フレーバーへ展開でき、NO なら150 method でも解決しない。

- **Primary KPI = 複数METHOD参加者数**(テーマ内で2つ以上の異なる method を made したユニークユーザー数)。8週で20人が必達
- 20人は統計的十分性ではなく「比較ループが回る規模に達した」というプロダクトKPI
- **王道候補1件は必達目標にしない。** 王道は結果であり、期限を切って作るものではない
- 危険指標: **METHOD投稿者数 > 2METHOD以上体験者数**(件数比較より鋭い。ユニークユーザー単位で見る)
- 「同じものを擦る」は運営の目的であってユーザーに見せる言葉ではない。ユーザーには「次はこの作り方を試してみよう」

## 実装済み(2026-08-18)
第一テーマの体験はアプリに入っている。`lib/theme.ts`(テーマ定義) / `lib/method-diff.ts`(差分の名指し・次の1台・設計空間の地図) /
`mix_experiences.comparison`(直接比較) / `theme_progress`・`theme_method_stats`(段階指標) / `/admin/theme`(進捗)。
**予想機能・自動推薦の作り込み・店舗導線・王道の門・通知は意図的に未実装。**
ロック(有料ノート)は `lib/lock.ts` の `LOCK_FEATURE_ENABLED=false` で非表示中。

## 決定済み仕様(未実装・着手にはユーザーの入力が要る)
**フレーバーの識別に表示名を使わない。Canonical Flavor ID を使う。**
`flavors.canonical_id`(例 `al-fakher-double-apple`)を正準キーとし、`combo_key` をそれで置き換える。
現在は日本語表示名依存(`al fakher|ダブルアップル`)で、このままでは世界共通の比較が成立しない。
**着手できない理由**: 86フレーバーの英語正式名を機械的に推測すると誤った正準キーが恒久的に残る。
`ラブ66` `アイスアクア` のような表記を勝手にローマ字化しない。**英語名の対応表をユーザーからもらってから実装する。**
